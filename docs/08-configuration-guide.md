# TraceForge Configuration Guide

Hierarchy:
1. CLI Arguments (`--host`, `--port`)
2. Environment Variables (`TRACEFORGE_HOST`, `TRACEFORGE_PORT`)
3. Configuration Files (`traceforge.yaml`, `traceforge.toml`, `traceforge.json`)
4. Default Settings
