"""Live trace-viewer dashboard — reserved for a future release.

The dashboard is planned to be a FastAPI + React application; per the
current SDK-core milestone, FastAPI/React integration is explicitly out
of scope, so this package ships only as a placeholder. See docs/roadmap.md.
"""
